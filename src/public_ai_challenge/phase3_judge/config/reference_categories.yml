# PLACEHOLDER coverage reference list — not the real eCH-0070 Gemeinde-level
# Leistungen inventory (docs/architecture/adr/0001). That dataset isn't
# wired into this repo yet (Scout's build plan explicitly defers it too —
# see docs/architecture/README.md's ADR status table).
#
# This list is deliberately small and sourced only from what's already in
# docs/architecture/CONTEXT.md and the delivered seed data's own category
# tags — NOT from Patrick's 31-tool capability matrix
# (pipeline/legacy/handoff/MCP_CAPABILITY_MATRIX.md), which this project is
# not treating as authoritative for now.
#
# Replace this file's contents wholesale once the real eCH-0070 Gemeinde
# Leistungen list is imported; the coverage.py code doesn't need to change.
categories:
  - residence_registration
  - residence_deregistration
  - building_application
  - facility_rental
  - office_hours
  - municipal_contact
