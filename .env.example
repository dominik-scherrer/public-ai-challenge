OPENAI_API_KEY=your_openai_api_key_here

# Apertus / sovereign inference endpoint — same variables used by
# pipeline/prototype/scraper.py's model adapter and pipeline/judge/llm.py's
# ensemble. Set either PUBLIC_AI_ENDPOINT (full URL) or PUBLIC_AI_BASE_URL
# (endpoint gets /chat/completions appended). PUBLIC_AI_API_KEY is optional
# depending on the deployment.
PUBLIC_AI_ENDPOINT=
PUBLIC_AI_BASE_URL=
PUBLIC_AI_API_KEY=
PUBLIC_AI_MODEL=
